{
  "name": "hono-swagger-ui-example",
  "version": "1.0.1",
  "description": "Example usage of hono-swagger-ui middleware",
  "main": "app.ts",
  "scripts": {
    "dev": "bun app.ts",
    "start": "bun app.ts",
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.2.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 